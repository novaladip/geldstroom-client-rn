import { StyleSheet, Dimensions } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    top: 100,
    left: 30,
    zIndex: 1,
    backgroundColor: '#fff',
    height: '80%',
    width: '85%',
    paddingTop: 40,
    borderRadius: 10,
    elevation: 10,
  },
});
