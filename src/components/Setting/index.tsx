import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const Setting: React.FC<{}> = () => {
  return (
    <SafeAreaView>
      <Text>Setting</Text>
    </SafeAreaView>
  );
};
